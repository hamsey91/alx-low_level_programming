#include <stdio.h>

/**
 * main - Entry point
 *
 * description: C program that use the function printf
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
